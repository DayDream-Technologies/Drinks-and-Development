/**
 * Site configuration. Update these values without touching component code.
 */

/** LinkedIn group URL – used for "Join our LinkedIn group" CTA */
export const LINKEDIN_GROUP_URL = 'https://www.linkedin.com/groups/14488620/'

/**
 * Google Calendar ID for the Events section.
 * Create a public calendar at calendar.google.com, then copy its ID from
 * Settings → Integrate calendar → Calendar ID (e.g. xxxxx@group.calendar.google.com).
 */
export const GOOGLE_CALENDAR_ID = ''

/**
 * Form submission endpoint.
 * - Formspree: create a form at formspree.io, use the form action URL here.
 * - Netlify Forms: use the form name you set in the form (e.g. "signup").
 */
export const SIGNUP_FORM_ENDPOINT = ''

/** Set to true if using Netlify Forms (form name in SIGNUP_FORM_ENDPOINT). */
export const USE_NETLIFY_FORMS = false
